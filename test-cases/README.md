# Test Cases

Use this folder for focused HTML/CSS/JavaScript cases that isolate edge behavior.

## Suggested naming

- `css-grid-overflow.md`
- `event-delegation-shadow-dom.md`
- `sticky-header-ios-safari.md`

Each case can include:

- A short Markdown description (`<name>.md`)
- A matching runnable sample folder (`<name>/`)
